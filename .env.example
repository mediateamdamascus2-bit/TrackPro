# Vercel Postgres (set automatically on Vercel when you create a Postgres DB)
DATABASE_URL=

# Auth.js (NextAuth)
AUTH_SECRET=

# Optional: used by Auth.js for some flows
NEXTAUTH_URL=

# OpenAI (optional, for AI assistant routes later)
# OPENAI_API_KEY=
